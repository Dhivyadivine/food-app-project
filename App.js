import React from 'react';
// Import the main application component
import SwiftDineApp from './SwiftDineApp'; 

/**
 * MainApp component serves as the wrapper that renders the core application logic.
 */
function MainApp() {
  return (
    // Render the main application component
    <SwiftDineApp />
  );
}

export default MainApp;